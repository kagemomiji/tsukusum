{
  "name": "tsukuoki",
  "version": "0.0.1",
  "description": "tsukuoki-receipe-parser",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ts": "tsc",
    "build": "npm run build-ts && if [ -d dist/resource ]; then rm -r dist/resource; fi && cp -r src/resource dist ",
    "start": "node dist/index.js $npm_config_url"
  },
  "author": "kagemomiji",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.30",
    "tslint": "^6.1.3"
  },
  "dependencies": {
    "@types/cheerio": "^0.22.31",
    "@types/config": "^0.0.41",
    "@types/ejs": "^3.1.0",
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.10",
    "config": "^3.3.7",
    "ejs": "^3.1.7",
    "html-pdf-node": "^1.0.7",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4",
    "zlib": "^1.0.5"
  }
}
